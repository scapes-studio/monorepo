{
  "name": "@scapes-studio/website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "kamal:setup": "set -a && . ./.env.production && set +a && kamal setup",
    "kamal:deploy": "set -a && . ./.env.production && set +a && kamal deploy"
  },
  "dependencies": {
    "@1001-digital/layers.base": "latest",
    "@1001-digital/layers.evm": "latest",
    "@scapes-studio/indexer": "workspace:^",
    "@ponder/client": "^0.16.1",
    "nuxt": "^4.2.2",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  }
}
